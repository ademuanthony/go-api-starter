// Code generated by SQLBoiler 4.7.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// CryptoAsset is an object representing the database table.
type CryptoAsset struct {
	ID        string `boil:"id" json:"id" toml:"id" yaml:"id"`
	AccountID string `boil:"account_id" json:"account_id" toml:"account_id" yaml:"account_id"`
	Currency  string `boil:"currency" json:"currency" toml:"currency" yaml:"currency"`
	Balance   int64  `boil:"balance" json:"balance" toml:"balance" yaml:"balance"`

	R *cryptoAssetR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L cryptoAssetL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CryptoAssetColumns = struct {
	ID        string
	AccountID string
	Currency  string
	Balance   string
}{
	ID:        "id",
	AccountID: "account_id",
	Currency:  "currency",
	Balance:   "balance",
}

var CryptoAssetTableColumns = struct {
	ID        string
	AccountID string
	Currency  string
	Balance   string
}{
	ID:        "crypto_asset.id",
	AccountID: "crypto_asset.account_id",
	Currency:  "crypto_asset.currency",
	Balance:   "crypto_asset.balance",
}

// Generated where

var CryptoAssetWhere = struct {
	ID        whereHelperstring
	AccountID whereHelperstring
	Currency  whereHelperstring
	Balance   whereHelperint64
}{
	ID:        whereHelperstring{field: "\"crypto_asset\".\"id\""},
	AccountID: whereHelperstring{field: "\"crypto_asset\".\"account_id\""},
	Currency:  whereHelperstring{field: "\"crypto_asset\".\"currency\""},
	Balance:   whereHelperint64{field: "\"crypto_asset\".\"balance\""},
}

// CryptoAssetRels is where relationship names are stored.
var CryptoAssetRels = struct {
	Account string
}{
	Account: "Account",
}

// cryptoAssetR is where relationships are stored.
type cryptoAssetR struct {
	Account *Account `boil:"Account" json:"Account" toml:"Account" yaml:"Account"`
}

// NewStruct creates a new relationship struct
func (*cryptoAssetR) NewStruct() *cryptoAssetR {
	return &cryptoAssetR{}
}

// cryptoAssetL is where Load methods for each relationship are stored.
type cryptoAssetL struct{}

var (
	cryptoAssetAllColumns            = []string{"id", "account_id", "currency", "balance"}
	cryptoAssetColumnsWithoutDefault = []string{"account_id", "currency", "balance"}
	cryptoAssetColumnsWithDefault    = []string{"id"}
	cryptoAssetPrimaryKeyColumns     = []string{"id"}
)

type (
	// CryptoAssetSlice is an alias for a slice of pointers to CryptoAsset.
	// This should almost always be used instead of []CryptoAsset.
	CryptoAssetSlice []*CryptoAsset

	cryptoAssetQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	cryptoAssetType                 = reflect.TypeOf(&CryptoAsset{})
	cryptoAssetMapping              = queries.MakeStructMapping(cryptoAssetType)
	cryptoAssetPrimaryKeyMapping, _ = queries.BindMapping(cryptoAssetType, cryptoAssetMapping, cryptoAssetPrimaryKeyColumns)
	cryptoAssetInsertCacheMut       sync.RWMutex
	cryptoAssetInsertCache          = make(map[string]insertCache)
	cryptoAssetUpdateCacheMut       sync.RWMutex
	cryptoAssetUpdateCache          = make(map[string]updateCache)
	cryptoAssetUpsertCacheMut       sync.RWMutex
	cryptoAssetUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single cryptoAsset record from the query.
func (q cryptoAssetQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CryptoAsset, error) {
	o := &CryptoAsset{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for crypto_asset")
	}

	return o, nil
}

// All returns all CryptoAsset records from the query.
func (q cryptoAssetQuery) All(ctx context.Context, exec boil.ContextExecutor) (CryptoAssetSlice, error) {
	var o []*CryptoAsset

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CryptoAsset slice")
	}

	return o, nil
}

// Count returns the count of all CryptoAsset records in the query.
func (q cryptoAssetQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count crypto_asset rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q cryptoAssetQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if crypto_asset exists")
	}

	return count > 0, nil
}

// Account pointed to by the foreign key.
func (o *CryptoAsset) Account(mods ...qm.QueryMod) accountQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.AccountID),
	}

	queryMods = append(queryMods, mods...)

	query := Accounts(queryMods...)
	queries.SetFrom(query.Query, "\"account\"")

	return query
}

// LoadAccount allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (cryptoAssetL) LoadAccount(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCryptoAsset interface{}, mods queries.Applicator) error {
	var slice []*CryptoAsset
	var object *CryptoAsset

	if singular {
		object = maybeCryptoAsset.(*CryptoAsset)
	} else {
		slice = *maybeCryptoAsset.(*[]*CryptoAsset)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &cryptoAssetR{}
		}
		args = append(args, object.AccountID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &cryptoAssetR{}
			}

			for _, a := range args {
				if a == obj.AccountID {
					continue Outer
				}
			}

			args = append(args, obj.AccountID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`account`),
		qm.WhereIn(`account.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Account")
	}

	var resultSlice []*Account
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Account")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for account")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for account")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Account = foreign
		if foreign.R == nil {
			foreign.R = &accountR{}
		}
		foreign.R.CryptoAssets = append(foreign.R.CryptoAssets, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.AccountID == foreign.ID {
				local.R.Account = foreign
				if foreign.R == nil {
					foreign.R = &accountR{}
				}
				foreign.R.CryptoAssets = append(foreign.R.CryptoAssets, local)
				break
			}
		}
	}

	return nil
}

// SetAccount of the cryptoAsset to the related item.
// Sets o.R.Account to related.
// Adds o to related.R.CryptoAssets.
func (o *CryptoAsset) SetAccount(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Account) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"crypto_asset\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"account_id"}),
		strmangle.WhereClause("\"", "\"", 2, cryptoAssetPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.AccountID = related.ID
	if o.R == nil {
		o.R = &cryptoAssetR{
			Account: related,
		}
	} else {
		o.R.Account = related
	}

	if related.R == nil {
		related.R = &accountR{
			CryptoAssets: CryptoAssetSlice{o},
		}
	} else {
		related.R.CryptoAssets = append(related.R.CryptoAssets, o)
	}

	return nil
}

// CryptoAssets retrieves all the records using an executor.
func CryptoAssets(mods ...qm.QueryMod) cryptoAssetQuery {
	mods = append(mods, qm.From("\"crypto_asset\""))
	return cryptoAssetQuery{NewQuery(mods...)}
}

// FindCryptoAsset retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCryptoAsset(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*CryptoAsset, error) {
	cryptoAssetObj := &CryptoAsset{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"crypto_asset\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, cryptoAssetObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from crypto_asset")
	}

	return cryptoAssetObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CryptoAsset) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no crypto_asset provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(cryptoAssetColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	cryptoAssetInsertCacheMut.RLock()
	cache, cached := cryptoAssetInsertCache[key]
	cryptoAssetInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			cryptoAssetAllColumns,
			cryptoAssetColumnsWithDefault,
			cryptoAssetColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(cryptoAssetType, cryptoAssetMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(cryptoAssetType, cryptoAssetMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"crypto_asset\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"crypto_asset\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into crypto_asset")
	}

	if !cached {
		cryptoAssetInsertCacheMut.Lock()
		cryptoAssetInsertCache[key] = cache
		cryptoAssetInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the CryptoAsset.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CryptoAsset) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	cryptoAssetUpdateCacheMut.RLock()
	cache, cached := cryptoAssetUpdateCache[key]
	cryptoAssetUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			cryptoAssetAllColumns,
			cryptoAssetPrimaryKeyColumns,
		)

		if len(wl) == 0 {
			return 0, errors.New("models: unable to update crypto_asset, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"crypto_asset\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, cryptoAssetPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(cryptoAssetType, cryptoAssetMapping, append(wl, cryptoAssetPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update crypto_asset row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for crypto_asset")
	}

	if !cached {
		cryptoAssetUpdateCacheMut.Lock()
		cryptoAssetUpdateCache[key] = cache
		cryptoAssetUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q cryptoAssetQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for crypto_asset")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for crypto_asset")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CryptoAssetSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cryptoAssetPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"crypto_asset\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, cryptoAssetPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in cryptoAsset slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all cryptoAsset")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CryptoAsset) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no crypto_asset provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(cryptoAssetColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	cryptoAssetUpsertCacheMut.RLock()
	cache, cached := cryptoAssetUpsertCache[key]
	cryptoAssetUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			cryptoAssetAllColumns,
			cryptoAssetColumnsWithDefault,
			cryptoAssetColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			cryptoAssetAllColumns,
			cryptoAssetPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert crypto_asset, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(cryptoAssetPrimaryKeyColumns))
			copy(conflict, cryptoAssetPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"crypto_asset\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(cryptoAssetType, cryptoAssetMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(cryptoAssetType, cryptoAssetMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert crypto_asset")
	}

	if !cached {
		cryptoAssetUpsertCacheMut.Lock()
		cryptoAssetUpsertCache[key] = cache
		cryptoAssetUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single CryptoAsset record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CryptoAsset) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CryptoAsset provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cryptoAssetPrimaryKeyMapping)
	sql := "DELETE FROM \"crypto_asset\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from crypto_asset")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for crypto_asset")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q cryptoAssetQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no cryptoAssetQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from crypto_asset")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for crypto_asset")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CryptoAssetSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cryptoAssetPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"crypto_asset\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, cryptoAssetPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from cryptoAsset slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for crypto_asset")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CryptoAsset) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCryptoAsset(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CryptoAssetSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CryptoAssetSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), cryptoAssetPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"crypto_asset\".* FROM \"crypto_asset\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, cryptoAssetPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CryptoAssetSlice")
	}

	*o = slice

	return nil
}

// CryptoAssetExists checks if the CryptoAsset row exists.
func CryptoAssetExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"crypto_asset\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if crypto_asset exists")
	}

	return exists, nil
}
