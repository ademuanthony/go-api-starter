// Code generated by SQLBoiler 4.7.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// DFCHolder is an object representing the database table.
type DFCHolder struct {
	WalletAddress string `boil:"wallet_address" json:"wallet_address" toml:"wallet_address" yaml:"wallet_address"`
	Balance       string `boil:"balance" json:"balance" toml:"balance" yaml:"balance"`
	Sent          int    `boil:"sent" json:"sent" toml:"sent" yaml:"sent"`
	Balance2      int64  `boil:"balance2" json:"balance2" toml:"balance2" yaml:"balance2"`
	Hash          string `boil:"hash" json:"hash" toml:"hash" yaml:"hash"`

	R *dfcHolderR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L dfcHolderL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DFCHolderColumns = struct {
	WalletAddress string
	Balance       string
	Sent          string
	Balance2      string
	Hash          string
}{
	WalletAddress: "wallet_address",
	Balance:       "balance",
	Sent:          "sent",
	Balance2:      "balance2",
	Hash:          "hash",
}

var DFCHolderTableColumns = struct {
	WalletAddress string
	Balance       string
	Sent          string
	Balance2      string
	Hash          string
}{
	WalletAddress: "dfc_holders.wallet_address",
	Balance:       "dfc_holders.balance",
	Sent:          "dfc_holders.sent",
	Balance2:      "dfc_holders.balance2",
	Hash:          "dfc_holders.hash",
}

// Generated where

var DFCHolderWhere = struct {
	WalletAddress whereHelperstring
	Balance       whereHelperstring
	Sent          whereHelperint
	Balance2      whereHelperint64
	Hash          whereHelperstring
}{
	WalletAddress: whereHelperstring{field: "\"dfc_holders\".\"wallet_address\""},
	Balance:       whereHelperstring{field: "\"dfc_holders\".\"balance\""},
	Sent:          whereHelperint{field: "\"dfc_holders\".\"sent\""},
	Balance2:      whereHelperint64{field: "\"dfc_holders\".\"balance2\""},
	Hash:          whereHelperstring{field: "\"dfc_holders\".\"hash\""},
}

// DFCHolderRels is where relationship names are stored.
var DFCHolderRels = struct {
}{}

// dfcHolderR is where relationships are stored.
type dfcHolderR struct {
}

// NewStruct creates a new relationship struct
func (*dfcHolderR) NewStruct() *dfcHolderR {
	return &dfcHolderR{}
}

// dfcHolderL is where Load methods for each relationship are stored.
type dfcHolderL struct{}

var (
	dfcHolderAllColumns            = []string{"wallet_address", "balance", "sent", "balance2", "hash"}
	dfcHolderColumnsWithoutDefault = []string{"wallet_address", "balance"}
	dfcHolderColumnsWithDefault    = []string{"sent", "balance2", "hash"}
	dfcHolderPrimaryKeyColumns     = []string{"wallet_address"}
)

type (
	// DFCHolderSlice is an alias for a slice of pointers to DFCHolder.
	// This should almost always be used instead of []DFCHolder.
	DFCHolderSlice []*DFCHolder

	dfcHolderQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	dfcHolderType                 = reflect.TypeOf(&DFCHolder{})
	dfcHolderMapping              = queries.MakeStructMapping(dfcHolderType)
	dfcHolderPrimaryKeyMapping, _ = queries.BindMapping(dfcHolderType, dfcHolderMapping, dfcHolderPrimaryKeyColumns)
	dfcHolderInsertCacheMut       sync.RWMutex
	dfcHolderInsertCache          = make(map[string]insertCache)
	dfcHolderUpdateCacheMut       sync.RWMutex
	dfcHolderUpdateCache          = make(map[string]updateCache)
	dfcHolderUpsertCacheMut       sync.RWMutex
	dfcHolderUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single dfcHolder record from the query.
func (q dfcHolderQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DFCHolder, error) {
	o := &DFCHolder{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for dfc_holders")
	}

	return o, nil
}

// All returns all DFCHolder records from the query.
func (q dfcHolderQuery) All(ctx context.Context, exec boil.ContextExecutor) (DFCHolderSlice, error) {
	var o []*DFCHolder

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to DFCHolder slice")
	}

	return o, nil
}

// Count returns the count of all DFCHolder records in the query.
func (q dfcHolderQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count dfc_holders rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q dfcHolderQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if dfc_holders exists")
	}

	return count > 0, nil
}

// DFCHolders retrieves all the records using an executor.
func DFCHolders(mods ...qm.QueryMod) dfcHolderQuery {
	mods = append(mods, qm.From("\"dfc_holders\""))
	return dfcHolderQuery{NewQuery(mods...)}
}

// FindDFCHolder retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDFCHolder(ctx context.Context, exec boil.ContextExecutor, walletAddress string, selectCols ...string) (*DFCHolder, error) {
	dfcHolderObj := &DFCHolder{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"dfc_holders\" where \"wallet_address\"=$1", sel,
	)

	q := queries.Raw(query, walletAddress)

	err := q.Bind(ctx, exec, dfcHolderObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from dfc_holders")
	}

	return dfcHolderObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DFCHolder) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no dfc_holders provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(dfcHolderColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	dfcHolderInsertCacheMut.RLock()
	cache, cached := dfcHolderInsertCache[key]
	dfcHolderInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			dfcHolderAllColumns,
			dfcHolderColumnsWithDefault,
			dfcHolderColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(dfcHolderType, dfcHolderMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(dfcHolderType, dfcHolderMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"dfc_holders\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"dfc_holders\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into dfc_holders")
	}

	if !cached {
		dfcHolderInsertCacheMut.Lock()
		dfcHolderInsertCache[key] = cache
		dfcHolderInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the DFCHolder.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DFCHolder) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	dfcHolderUpdateCacheMut.RLock()
	cache, cached := dfcHolderUpdateCache[key]
	dfcHolderUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			dfcHolderAllColumns,
			dfcHolderPrimaryKeyColumns,
		)

		if len(wl) == 0 {
			return 0, errors.New("models: unable to update dfc_holders, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"dfc_holders\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, dfcHolderPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(dfcHolderType, dfcHolderMapping, append(wl, dfcHolderPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update dfc_holders row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for dfc_holders")
	}

	if !cached {
		dfcHolderUpdateCacheMut.Lock()
		dfcHolderUpdateCache[key] = cache
		dfcHolderUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q dfcHolderQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for dfc_holders")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for dfc_holders")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DFCHolderSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dfcHolderPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"dfc_holders\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, dfcHolderPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in dfcHolder slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all dfcHolder")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DFCHolder) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no dfc_holders provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(dfcHolderColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	dfcHolderUpsertCacheMut.RLock()
	cache, cached := dfcHolderUpsertCache[key]
	dfcHolderUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			dfcHolderAllColumns,
			dfcHolderColumnsWithDefault,
			dfcHolderColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			dfcHolderAllColumns,
			dfcHolderPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert dfc_holders, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(dfcHolderPrimaryKeyColumns))
			copy(conflict, dfcHolderPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"dfc_holders\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(dfcHolderType, dfcHolderMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(dfcHolderType, dfcHolderMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert dfc_holders")
	}

	if !cached {
		dfcHolderUpsertCacheMut.Lock()
		dfcHolderUpsertCache[key] = cache
		dfcHolderUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single DFCHolder record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DFCHolder) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no DFCHolder provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), dfcHolderPrimaryKeyMapping)
	sql := "DELETE FROM \"dfc_holders\" WHERE \"wallet_address\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from dfc_holders")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for dfc_holders")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q dfcHolderQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no dfcHolderQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from dfc_holders")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for dfc_holders")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DFCHolderSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dfcHolderPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"dfc_holders\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, dfcHolderPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from dfcHolder slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for dfc_holders")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DFCHolder) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDFCHolder(ctx, exec, o.WalletAddress)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DFCHolderSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DFCHolderSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dfcHolderPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"dfc_holders\".* FROM \"dfc_holders\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, dfcHolderPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in DFCHolderSlice")
	}

	*o = slice

	return nil
}

// DFCHolderExists checks if the DFCHolder row exists.
func DFCHolderExists(ctx context.Context, exec boil.ContextExecutor, walletAddress string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"dfc_holders\" where \"wallet_address\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, walletAddress)
	}
	row := exec.QueryRowContext(ctx, sql, walletAddress)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if dfc_holders exists")
	}

	return exists, nil
}
