// Code generated by SQLBoiler 4.7.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// DFCWalletTransaction is an object representing the database table.
type DFCWalletTransaction struct {
	ID            string  `boil:"id" json:"id" toml:"id" yaml:"id"`
	WalletAddress string  `boil:"wallet_address" json:"wallet_address" toml:"wallet_address" yaml:"wallet_address"`
	Amount        float64 `boil:"amount" json:"amount" toml:"amount" yaml:"amount"`

	R *dfcWalletTransactionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L dfcWalletTransactionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DFCWalletTransactionColumns = struct {
	ID            string
	WalletAddress string
	Amount        string
}{
	ID:            "id",
	WalletAddress: "wallet_address",
	Amount:        "amount",
}

var DFCWalletTransactionTableColumns = struct {
	ID            string
	WalletAddress string
	Amount        string
}{
	ID:            "dfc_wallet_transaction.id",
	WalletAddress: "dfc_wallet_transaction.wallet_address",
	Amount:        "dfc_wallet_transaction.amount",
}

// Generated where

type whereHelperfloat64 struct{ field string }

func (w whereHelperfloat64) EQ(x float64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperfloat64) NEQ(x float64) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelperfloat64) LT(x float64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperfloat64) LTE(x float64) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelperfloat64) GT(x float64) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperfloat64) GTE(x float64) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}
func (w whereHelperfloat64) IN(slice []float64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperfloat64) NIN(slice []float64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

var DFCWalletTransactionWhere = struct {
	ID            whereHelperstring
	WalletAddress whereHelperstring
	Amount        whereHelperfloat64
}{
	ID:            whereHelperstring{field: "\"dfc_wallet_transaction\".\"id\""},
	WalletAddress: whereHelperstring{field: "\"dfc_wallet_transaction\".\"wallet_address\""},
	Amount:        whereHelperfloat64{field: "\"dfc_wallet_transaction\".\"amount\""},
}

// DFCWalletTransactionRels is where relationship names are stored.
var DFCWalletTransactionRels = struct {
}{}

// dfcWalletTransactionR is where relationships are stored.
type dfcWalletTransactionR struct {
}

// NewStruct creates a new relationship struct
func (*dfcWalletTransactionR) NewStruct() *dfcWalletTransactionR {
	return &dfcWalletTransactionR{}
}

// dfcWalletTransactionL is where Load methods for each relationship are stored.
type dfcWalletTransactionL struct{}

var (
	dfcWalletTransactionAllColumns            = []string{"id", "wallet_address", "amount"}
	dfcWalletTransactionColumnsWithoutDefault = []string{"wallet_address", "amount"}
	dfcWalletTransactionColumnsWithDefault    = []string{"id"}
	dfcWalletTransactionPrimaryKeyColumns     = []string{"id"}
)

type (
	// DFCWalletTransactionSlice is an alias for a slice of pointers to DFCWalletTransaction.
	// This should almost always be used instead of []DFCWalletTransaction.
	DFCWalletTransactionSlice []*DFCWalletTransaction

	dfcWalletTransactionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	dfcWalletTransactionType                 = reflect.TypeOf(&DFCWalletTransaction{})
	dfcWalletTransactionMapping              = queries.MakeStructMapping(dfcWalletTransactionType)
	dfcWalletTransactionPrimaryKeyMapping, _ = queries.BindMapping(dfcWalletTransactionType, dfcWalletTransactionMapping, dfcWalletTransactionPrimaryKeyColumns)
	dfcWalletTransactionInsertCacheMut       sync.RWMutex
	dfcWalletTransactionInsertCache          = make(map[string]insertCache)
	dfcWalletTransactionUpdateCacheMut       sync.RWMutex
	dfcWalletTransactionUpdateCache          = make(map[string]updateCache)
	dfcWalletTransactionUpsertCacheMut       sync.RWMutex
	dfcWalletTransactionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single dfcWalletTransaction record from the query.
func (q dfcWalletTransactionQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DFCWalletTransaction, error) {
	o := &DFCWalletTransaction{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for dfc_wallet_transaction")
	}

	return o, nil
}

// All returns all DFCWalletTransaction records from the query.
func (q dfcWalletTransactionQuery) All(ctx context.Context, exec boil.ContextExecutor) (DFCWalletTransactionSlice, error) {
	var o []*DFCWalletTransaction

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to DFCWalletTransaction slice")
	}

	return o, nil
}

// Count returns the count of all DFCWalletTransaction records in the query.
func (q dfcWalletTransactionQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count dfc_wallet_transaction rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q dfcWalletTransactionQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if dfc_wallet_transaction exists")
	}

	return count > 0, nil
}

// DFCWalletTransactions retrieves all the records using an executor.
func DFCWalletTransactions(mods ...qm.QueryMod) dfcWalletTransactionQuery {
	mods = append(mods, qm.From("\"dfc_wallet_transaction\""))
	return dfcWalletTransactionQuery{NewQuery(mods...)}
}

// FindDFCWalletTransaction retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDFCWalletTransaction(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*DFCWalletTransaction, error) {
	dfcWalletTransactionObj := &DFCWalletTransaction{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"dfc_wallet_transaction\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, dfcWalletTransactionObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from dfc_wallet_transaction")
	}

	return dfcWalletTransactionObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DFCWalletTransaction) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no dfc_wallet_transaction provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(dfcWalletTransactionColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	dfcWalletTransactionInsertCacheMut.RLock()
	cache, cached := dfcWalletTransactionInsertCache[key]
	dfcWalletTransactionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			dfcWalletTransactionAllColumns,
			dfcWalletTransactionColumnsWithDefault,
			dfcWalletTransactionColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(dfcWalletTransactionType, dfcWalletTransactionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(dfcWalletTransactionType, dfcWalletTransactionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"dfc_wallet_transaction\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"dfc_wallet_transaction\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into dfc_wallet_transaction")
	}

	if !cached {
		dfcWalletTransactionInsertCacheMut.Lock()
		dfcWalletTransactionInsertCache[key] = cache
		dfcWalletTransactionInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the DFCWalletTransaction.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DFCWalletTransaction) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	dfcWalletTransactionUpdateCacheMut.RLock()
	cache, cached := dfcWalletTransactionUpdateCache[key]
	dfcWalletTransactionUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			dfcWalletTransactionAllColumns,
			dfcWalletTransactionPrimaryKeyColumns,
		)

		if len(wl) == 0 {
			return 0, errors.New("models: unable to update dfc_wallet_transaction, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"dfc_wallet_transaction\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, dfcWalletTransactionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(dfcWalletTransactionType, dfcWalletTransactionMapping, append(wl, dfcWalletTransactionPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update dfc_wallet_transaction row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for dfc_wallet_transaction")
	}

	if !cached {
		dfcWalletTransactionUpdateCacheMut.Lock()
		dfcWalletTransactionUpdateCache[key] = cache
		dfcWalletTransactionUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q dfcWalletTransactionQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for dfc_wallet_transaction")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for dfc_wallet_transaction")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DFCWalletTransactionSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dfcWalletTransactionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"dfc_wallet_transaction\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, dfcWalletTransactionPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in dfcWalletTransaction slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all dfcWalletTransaction")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DFCWalletTransaction) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no dfc_wallet_transaction provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(dfcWalletTransactionColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	dfcWalletTransactionUpsertCacheMut.RLock()
	cache, cached := dfcWalletTransactionUpsertCache[key]
	dfcWalletTransactionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			dfcWalletTransactionAllColumns,
			dfcWalletTransactionColumnsWithDefault,
			dfcWalletTransactionColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			dfcWalletTransactionAllColumns,
			dfcWalletTransactionPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert dfc_wallet_transaction, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(dfcWalletTransactionPrimaryKeyColumns))
			copy(conflict, dfcWalletTransactionPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"dfc_wallet_transaction\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(dfcWalletTransactionType, dfcWalletTransactionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(dfcWalletTransactionType, dfcWalletTransactionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert dfc_wallet_transaction")
	}

	if !cached {
		dfcWalletTransactionUpsertCacheMut.Lock()
		dfcWalletTransactionUpsertCache[key] = cache
		dfcWalletTransactionUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single DFCWalletTransaction record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DFCWalletTransaction) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no DFCWalletTransaction provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), dfcWalletTransactionPrimaryKeyMapping)
	sql := "DELETE FROM \"dfc_wallet_transaction\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from dfc_wallet_transaction")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for dfc_wallet_transaction")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q dfcWalletTransactionQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no dfcWalletTransactionQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from dfc_wallet_transaction")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for dfc_wallet_transaction")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DFCWalletTransactionSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dfcWalletTransactionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"dfc_wallet_transaction\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, dfcWalletTransactionPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from dfcWalletTransaction slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for dfc_wallet_transaction")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DFCWalletTransaction) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDFCWalletTransaction(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DFCWalletTransactionSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DFCWalletTransactionSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dfcWalletTransactionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"dfc_wallet_transaction\".* FROM \"dfc_wallet_transaction\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, dfcWalletTransactionPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in DFCWalletTransactionSlice")
	}

	*o = slice

	return nil
}

// DFCWalletTransactionExists checks if the DFCWalletTransaction row exists.
func DFCWalletTransactionExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"dfc_wallet_transaction\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if dfc_wallet_transaction exists")
	}

	return exists, nil
}
