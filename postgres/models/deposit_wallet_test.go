// Code generated by SQLBoiler 4.7.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testDepositWallets(t *testing.T) {
	t.Parallel()

	query := DepositWallets()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testDepositWalletsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DepositWallet{}
	if err = randomize.Struct(seed, o, depositWalletDBTypes, true, depositWalletColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DepositWallets().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDepositWalletsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DepositWallet{}
	if err = randomize.Struct(seed, o, depositWalletDBTypes, true, depositWalletColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := DepositWallets().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DepositWallets().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDepositWalletsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DepositWallet{}
	if err = randomize.Struct(seed, o, depositWalletDBTypes, true, depositWalletColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := DepositWalletSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DepositWallets().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDepositWalletsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DepositWallet{}
	if err = randomize.Struct(seed, o, depositWalletDBTypes, true, depositWalletColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := DepositWalletExists(ctx, tx, o.Address)
	if err != nil {
		t.Errorf("Unable to check if DepositWallet exists: %s", err)
	}
	if !e {
		t.Errorf("Expected DepositWalletExists to return true, but got false.")
	}
}

func testDepositWalletsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DepositWallet{}
	if err = randomize.Struct(seed, o, depositWalletDBTypes, true, depositWalletColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	depositWalletFound, err := FindDepositWallet(ctx, tx, o.Address)
	if err != nil {
		t.Error(err)
	}

	if depositWalletFound == nil {
		t.Error("want a record, got nil")
	}
}

func testDepositWalletsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DepositWallet{}
	if err = randomize.Struct(seed, o, depositWalletDBTypes, true, depositWalletColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = DepositWallets().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testDepositWalletsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DepositWallet{}
	if err = randomize.Struct(seed, o, depositWalletDBTypes, true, depositWalletColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := DepositWallets().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testDepositWalletsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	depositWalletOne := &DepositWallet{}
	depositWalletTwo := &DepositWallet{}
	if err = randomize.Struct(seed, depositWalletOne, depositWalletDBTypes, false, depositWalletColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}
	if err = randomize.Struct(seed, depositWalletTwo, depositWalletDBTypes, false, depositWalletColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = depositWalletOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = depositWalletTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := DepositWallets().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testDepositWalletsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	depositWalletOne := &DepositWallet{}
	depositWalletTwo := &DepositWallet{}
	if err = randomize.Struct(seed, depositWalletOne, depositWalletDBTypes, false, depositWalletColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}
	if err = randomize.Struct(seed, depositWalletTwo, depositWalletDBTypes, false, depositWalletColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = depositWalletOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = depositWalletTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DepositWallets().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testDepositWalletsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DepositWallet{}
	if err = randomize.Struct(seed, o, depositWalletDBTypes, true, depositWalletColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DepositWallets().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testDepositWalletsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DepositWallet{}
	if err = randomize.Struct(seed, o, depositWalletDBTypes, true); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(depositWalletColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := DepositWallets().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testDepositWalletToManyWalletAddressCryptoDeposits(t *testing.T) {
	var err error
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a DepositWallet
	var b, c CryptoDeposit

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, depositWalletDBTypes, true, depositWalletColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, cryptoDepositDBTypes, false, cryptoDepositColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, cryptoDepositDBTypes, false, cryptoDepositColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	b.WalletAddress = a.Address
	c.WalletAddress = a.Address

	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.WalletAddressCryptoDeposits().All(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if v.WalletAddress == b.WalletAddress {
			bFound = true
		}
		if v.WalletAddress == c.WalletAddress {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := DepositWalletSlice{&a}
	if err = a.L.LoadWalletAddressCryptoDeposits(ctx, tx, false, (*[]*DepositWallet)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.WalletAddressCryptoDeposits); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.WalletAddressCryptoDeposits = nil
	if err = a.L.LoadWalletAddressCryptoDeposits(ctx, tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.WalletAddressCryptoDeposits); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testDepositWalletToManyWalletAddressWalletBalanaces(t *testing.T) {
	var err error
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a DepositWallet
	var b, c WalletBalanace

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, depositWalletDBTypes, true, depositWalletColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, walletBalanaceDBTypes, false, walletBalanaceColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, walletBalanaceDBTypes, false, walletBalanaceColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	b.WalletAddress = a.Address
	c.WalletAddress = a.Address

	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.WalletAddressWalletBalanaces().All(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if v.WalletAddress == b.WalletAddress {
			bFound = true
		}
		if v.WalletAddress == c.WalletAddress {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := DepositWalletSlice{&a}
	if err = a.L.LoadWalletAddressWalletBalanaces(ctx, tx, false, (*[]*DepositWallet)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.WalletAddressWalletBalanaces); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.WalletAddressWalletBalanaces = nil
	if err = a.L.LoadWalletAddressWalletBalanaces(ctx, tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.WalletAddressWalletBalanaces); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testDepositWalletToManyAddOpWalletAddressCryptoDeposits(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a DepositWallet
	var b, c, d, e CryptoDeposit

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, depositWalletDBTypes, false, strmangle.SetComplement(depositWalletPrimaryKeyColumns, depositWalletColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*CryptoDeposit{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, cryptoDepositDBTypes, false, strmangle.SetComplement(cryptoDepositPrimaryKeyColumns, cryptoDepositColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*CryptoDeposit{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddWalletAddressCryptoDeposits(ctx, tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if a.Address != first.WalletAddress {
			t.Error("foreign key was wrong value", a.Address, first.WalletAddress)
		}
		if a.Address != second.WalletAddress {
			t.Error("foreign key was wrong value", a.Address, second.WalletAddress)
		}

		if first.R.WalletAddressDepositWallet != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.WalletAddressDepositWallet != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.WalletAddressCryptoDeposits[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.WalletAddressCryptoDeposits[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.WalletAddressCryptoDeposits().Count(ctx, tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}
func testDepositWalletToManyAddOpWalletAddressWalletBalanaces(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a DepositWallet
	var b, c, d, e WalletBalanace

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, depositWalletDBTypes, false, strmangle.SetComplement(depositWalletPrimaryKeyColumns, depositWalletColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*WalletBalanace{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, walletBalanaceDBTypes, false, strmangle.SetComplement(walletBalanacePrimaryKeyColumns, walletBalanaceColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*WalletBalanace{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddWalletAddressWalletBalanaces(ctx, tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if a.Address != first.WalletAddress {
			t.Error("foreign key was wrong value", a.Address, first.WalletAddress)
		}
		if a.Address != second.WalletAddress {
			t.Error("foreign key was wrong value", a.Address, second.WalletAddress)
		}

		if first.R.WalletAddressDepositWallet != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.WalletAddressDepositWallet != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.WalletAddressWalletBalanaces[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.WalletAddressWalletBalanaces[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.WalletAddressWalletBalanaces().Count(ctx, tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}
func testDepositWalletToOneAccountUsingAccount(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local DepositWallet
	var foreign Account

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, depositWalletDBTypes, false, depositWalletColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, accountDBTypes, false, accountColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Account struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.AccountID = foreign.ID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Account().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := DepositWalletSlice{&local}
	if err = local.L.LoadAccount(ctx, tx, false, (*[]*DepositWallet)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Account == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Account = nil
	if err = local.L.LoadAccount(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Account == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testDepositWalletToOneSetOpAccountUsingAccount(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a DepositWallet
	var b, c Account

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, depositWalletDBTypes, false, strmangle.SetComplement(depositWalletPrimaryKeyColumns, depositWalletColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, accountDBTypes, false, strmangle.SetComplement(accountPrimaryKeyColumns, accountColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, accountDBTypes, false, strmangle.SetComplement(accountPrimaryKeyColumns, accountColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Account{&b, &c} {
		err = a.SetAccount(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Account != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.DepositWallets[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.AccountID != x.ID {
			t.Error("foreign key was wrong value", a.AccountID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.AccountID))
		reflect.Indirect(reflect.ValueOf(&a.AccountID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.AccountID != x.ID {
			t.Error("foreign key was wrong value", a.AccountID, x.ID)
		}
	}
}

func testDepositWalletsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DepositWallet{}
	if err = randomize.Struct(seed, o, depositWalletDBTypes, true, depositWalletColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testDepositWalletsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DepositWallet{}
	if err = randomize.Struct(seed, o, depositWalletDBTypes, true, depositWalletColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := DepositWalletSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testDepositWalletsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DepositWallet{}
	if err = randomize.Struct(seed, o, depositWalletDBTypes, true, depositWalletColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := DepositWallets().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	depositWalletDBTypes = map[string]string{`Address`: `character varying`, `PrivateKey`: `character varying`, `AccountID`: `character varying`, `Blockchain`: `character varying`}
	_                    = bytes.MinRead
)

func testDepositWalletsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(depositWalletPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(depositWalletAllColumns) == len(depositWalletPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &DepositWallet{}
	if err = randomize.Struct(seed, o, depositWalletDBTypes, true, depositWalletColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DepositWallets().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, depositWalletDBTypes, true, depositWalletPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testDepositWalletsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(depositWalletAllColumns) == len(depositWalletPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &DepositWallet{}
	if err = randomize.Struct(seed, o, depositWalletDBTypes, true, depositWalletColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DepositWallets().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, depositWalletDBTypes, true, depositWalletPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(depositWalletAllColumns, depositWalletPrimaryKeyColumns) {
		fields = depositWalletAllColumns
	} else {
		fields = strmangle.SetComplement(
			depositWalletAllColumns,
			depositWalletPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := DepositWalletSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testDepositWalletsUpsert(t *testing.T) {
	t.Parallel()

	if len(depositWalletAllColumns) == len(depositWalletPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := DepositWallet{}
	if err = randomize.Struct(seed, &o, depositWalletDBTypes, true); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert DepositWallet: %s", err)
	}

	count, err := DepositWallets().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, depositWalletDBTypes, false, depositWalletPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DepositWallet struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert DepositWallet: %s", err)
	}

	count, err = DepositWallets().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
