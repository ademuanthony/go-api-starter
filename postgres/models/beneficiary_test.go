// Code generated by SQLBoiler 4.7.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testBeneficiaries(t *testing.T) {
	t.Parallel()

	query := Beneficiaries()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testBeneficiariesDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Beneficiary{}
	if err = randomize.Struct(seed, o, beneficiaryDBTypes, true, beneficiaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Beneficiaries().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testBeneficiariesQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Beneficiary{}
	if err = randomize.Struct(seed, o, beneficiaryDBTypes, true, beneficiaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := Beneficiaries().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Beneficiaries().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testBeneficiariesSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Beneficiary{}
	if err = randomize.Struct(seed, o, beneficiaryDBTypes, true, beneficiaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := BeneficiarySlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Beneficiaries().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testBeneficiariesExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Beneficiary{}
	if err = randomize.Struct(seed, o, beneficiaryDBTypes, true, beneficiaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := BeneficiaryExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if Beneficiary exists: %s", err)
	}
	if !e {
		t.Errorf("Expected BeneficiaryExists to return true, but got false.")
	}
}

func testBeneficiariesFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Beneficiary{}
	if err = randomize.Struct(seed, o, beneficiaryDBTypes, true, beneficiaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	beneficiaryFound, err := FindBeneficiary(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if beneficiaryFound == nil {
		t.Error("want a record, got nil")
	}
}

func testBeneficiariesBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Beneficiary{}
	if err = randomize.Struct(seed, o, beneficiaryDBTypes, true, beneficiaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = Beneficiaries().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testBeneficiariesOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Beneficiary{}
	if err = randomize.Struct(seed, o, beneficiaryDBTypes, true, beneficiaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := Beneficiaries().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testBeneficiariesAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	beneficiaryOne := &Beneficiary{}
	beneficiaryTwo := &Beneficiary{}
	if err = randomize.Struct(seed, beneficiaryOne, beneficiaryDBTypes, false, beneficiaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}
	if err = randomize.Struct(seed, beneficiaryTwo, beneficiaryDBTypes, false, beneficiaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = beneficiaryOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = beneficiaryTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Beneficiaries().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testBeneficiariesCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	beneficiaryOne := &Beneficiary{}
	beneficiaryTwo := &Beneficiary{}
	if err = randomize.Struct(seed, beneficiaryOne, beneficiaryDBTypes, false, beneficiaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}
	if err = randomize.Struct(seed, beneficiaryTwo, beneficiaryDBTypes, false, beneficiaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = beneficiaryOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = beneficiaryTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Beneficiaries().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testBeneficiariesInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Beneficiary{}
	if err = randomize.Struct(seed, o, beneficiaryDBTypes, true, beneficiaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Beneficiaries().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testBeneficiariesInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Beneficiary{}
	if err = randomize.Struct(seed, o, beneficiaryDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(beneficiaryColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := Beneficiaries().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testBeneficiaryToOneAccountUsingAccount(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local Beneficiary
	var foreign Account

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, beneficiaryDBTypes, true, beneficiaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, accountDBTypes, false, accountColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Account struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.AccountID, foreign.ID)
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Account().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.ID, foreign.ID) {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := BeneficiarySlice{&local}
	if err = local.L.LoadAccount(ctx, tx, false, (*[]*Beneficiary)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Account == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Account = nil
	if err = local.L.LoadAccount(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Account == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testBeneficiaryToOneSetOpAccountUsingAccount(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Beneficiary
	var b, c Account

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, beneficiaryDBTypes, false, strmangle.SetComplement(beneficiaryPrimaryKeyColumns, beneficiaryColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, accountDBTypes, false, strmangle.SetComplement(accountPrimaryKeyColumns, accountColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, accountDBTypes, false, strmangle.SetComplement(accountPrimaryKeyColumns, accountColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Account{&b, &c} {
		err = a.SetAccount(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Account != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.Beneficiaries[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.AccountID, x.ID) {
			t.Error("foreign key was wrong value", a.AccountID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.AccountID))
		reflect.Indirect(reflect.ValueOf(&a.AccountID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.AccountID, x.ID) {
			t.Error("foreign key was wrong value", a.AccountID, x.ID)
		}
	}
}

func testBeneficiaryToOneRemoveOpAccountUsingAccount(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Beneficiary
	var b Account

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, beneficiaryDBTypes, false, strmangle.SetComplement(beneficiaryPrimaryKeyColumns, beneficiaryColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, accountDBTypes, false, strmangle.SetComplement(accountPrimaryKeyColumns, accountColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetAccount(ctx, tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemoveAccount(ctx, tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.Account().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.Account != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.AccountID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.Beneficiaries) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testBeneficiariesReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Beneficiary{}
	if err = randomize.Struct(seed, o, beneficiaryDBTypes, true, beneficiaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testBeneficiariesReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Beneficiary{}
	if err = randomize.Struct(seed, o, beneficiaryDBTypes, true, beneficiaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := BeneficiarySlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testBeneficiariesSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Beneficiary{}
	if err = randomize.Struct(seed, o, beneficiaryDBTypes, true, beneficiaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Beneficiaries().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	beneficiaryDBTypes = map[string]string{`ID`: `uuid`, `AccountID`: `character varying`, `Bank`: `character varying`, `AccountNumber`: `character varying`, `AccountName`: `character varying`, `Country`: `character varying`, `BeneficialEmail`: `character varying`, `PhoneNumber`: `character varying`, `SmartCardNumber`: `character varying`, `MeterNumber`: `character varying`, `Type`: `integer`, `Date`: `bigint`}
	_                  = bytes.MinRead
)

func testBeneficiariesUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(beneficiaryPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(beneficiaryAllColumns) == len(beneficiaryPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Beneficiary{}
	if err = randomize.Struct(seed, o, beneficiaryDBTypes, true, beneficiaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Beneficiaries().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, beneficiaryDBTypes, true, beneficiaryPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testBeneficiariesSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(beneficiaryAllColumns) == len(beneficiaryPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Beneficiary{}
	if err = randomize.Struct(seed, o, beneficiaryDBTypes, true, beneficiaryColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Beneficiaries().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, beneficiaryDBTypes, true, beneficiaryPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(beneficiaryAllColumns, beneficiaryPrimaryKeyColumns) {
		fields = beneficiaryAllColumns
	} else {
		fields = strmangle.SetComplement(
			beneficiaryAllColumns,
			beneficiaryPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := BeneficiarySlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testBeneficiariesUpsert(t *testing.T) {
	t.Parallel()

	if len(beneficiaryAllColumns) == len(beneficiaryPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := Beneficiary{}
	if err = randomize.Struct(seed, &o, beneficiaryDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Beneficiary: %s", err)
	}

	count, err := Beneficiaries().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, beneficiaryDBTypes, false, beneficiaryPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Beneficiary struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Beneficiary: %s", err)
	}

	count, err = Beneficiaries().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
