// Code generated by SQLBoiler 4.7.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testTransfers(t *testing.T) {
	t.Parallel()

	query := Transfers()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testTransfersDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Transfer{}
	if err = randomize.Struct(seed, o, transferDBTypes, true, transferColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Transfers().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testTransfersQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Transfer{}
	if err = randomize.Struct(seed, o, transferDBTypes, true, transferColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := Transfers().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Transfers().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testTransfersSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Transfer{}
	if err = randomize.Struct(seed, o, transferDBTypes, true, transferColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := TransferSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Transfers().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testTransfersExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Transfer{}
	if err = randomize.Struct(seed, o, transferDBTypes, true, transferColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := TransferExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if Transfer exists: %s", err)
	}
	if !e {
		t.Errorf("Expected TransferExists to return true, but got false.")
	}
}

func testTransfersFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Transfer{}
	if err = randomize.Struct(seed, o, transferDBTypes, true, transferColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	transferFound, err := FindTransfer(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if transferFound == nil {
		t.Error("want a record, got nil")
	}
}

func testTransfersBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Transfer{}
	if err = randomize.Struct(seed, o, transferDBTypes, true, transferColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = Transfers().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testTransfersOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Transfer{}
	if err = randomize.Struct(seed, o, transferDBTypes, true, transferColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := Transfers().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testTransfersAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	transferOne := &Transfer{}
	transferTwo := &Transfer{}
	if err = randomize.Struct(seed, transferOne, transferDBTypes, false, transferColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}
	if err = randomize.Struct(seed, transferTwo, transferDBTypes, false, transferColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = transferOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = transferTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Transfers().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testTransfersCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	transferOne := &Transfer{}
	transferTwo := &Transfer{}
	if err = randomize.Struct(seed, transferOne, transferDBTypes, false, transferColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}
	if err = randomize.Struct(seed, transferTwo, transferDBTypes, false, transferColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = transferOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = transferTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Transfers().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func testTransfersInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Transfer{}
	if err = randomize.Struct(seed, o, transferDBTypes, true, transferColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Transfers().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testTransfersInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Transfer{}
	if err = randomize.Struct(seed, o, transferDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(transferColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := Transfers().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testTransferToOneAccountUsingReceiver(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local Transfer
	var foreign Account

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, transferDBTypes, false, transferColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, accountDBTypes, false, accountColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Account struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.ReceiverID = foreign.ID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Receiver().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := TransferSlice{&local}
	if err = local.L.LoadReceiver(ctx, tx, false, (*[]*Transfer)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Receiver == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Receiver = nil
	if err = local.L.LoadReceiver(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Receiver == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testTransferToOneAccountUsingSender(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local Transfer
	var foreign Account

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, transferDBTypes, false, transferColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, accountDBTypes, false, accountColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Account struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.SenderID = foreign.ID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Sender().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := TransferSlice{&local}
	if err = local.L.LoadSender(ctx, tx, false, (*[]*Transfer)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Sender == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Sender = nil
	if err = local.L.LoadSender(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Sender == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testTransferToOneSetOpAccountUsingReceiver(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Transfer
	var b, c Account

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, transferDBTypes, false, strmangle.SetComplement(transferPrimaryKeyColumns, transferColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, accountDBTypes, false, strmangle.SetComplement(accountPrimaryKeyColumns, accountColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, accountDBTypes, false, strmangle.SetComplement(accountPrimaryKeyColumns, accountColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Account{&b, &c} {
		err = a.SetReceiver(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Receiver != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.ReceiverTransfers[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.ReceiverID != x.ID {
			t.Error("foreign key was wrong value", a.ReceiverID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.ReceiverID))
		reflect.Indirect(reflect.ValueOf(&a.ReceiverID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.ReceiverID != x.ID {
			t.Error("foreign key was wrong value", a.ReceiverID, x.ID)
		}
	}
}
func testTransferToOneSetOpAccountUsingSender(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Transfer
	var b, c Account

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, transferDBTypes, false, strmangle.SetComplement(transferPrimaryKeyColumns, transferColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, accountDBTypes, false, strmangle.SetComplement(accountPrimaryKeyColumns, accountColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, accountDBTypes, false, strmangle.SetComplement(accountPrimaryKeyColumns, accountColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Account{&b, &c} {
		err = a.SetSender(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Sender != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.SenderTransfers[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.SenderID != x.ID {
			t.Error("foreign key was wrong value", a.SenderID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.SenderID))
		reflect.Indirect(reflect.ValueOf(&a.SenderID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.SenderID != x.ID {
			t.Error("foreign key was wrong value", a.SenderID, x.ID)
		}
	}
}

func testTransfersReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Transfer{}
	if err = randomize.Struct(seed, o, transferDBTypes, true, transferColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testTransfersReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Transfer{}
	if err = randomize.Struct(seed, o, transferDBTypes, true, transferColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := TransferSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testTransfersSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Transfer{}
	if err = randomize.Struct(seed, o, transferDBTypes, true, transferColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Transfers().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	transferDBTypes = map[string]string{`ID`: `character varying`, `Amount`: `bigint`, `SenderID`: `character varying`, `ReceiverID`: `character varying`, `Date`: `bigint`}
	_               = bytes.MinRead
)

func testTransfersUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(transferPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(transferAllColumns) == len(transferPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Transfer{}
	if err = randomize.Struct(seed, o, transferDBTypes, true, transferColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Transfers().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, transferDBTypes, true, transferPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testTransfersSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(transferAllColumns) == len(transferPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Transfer{}
	if err = randomize.Struct(seed, o, transferDBTypes, true, transferColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Transfers().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, transferDBTypes, true, transferPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(transferAllColumns, transferPrimaryKeyColumns) {
		fields = transferAllColumns
	} else {
		fields = strmangle.SetComplement(
			transferAllColumns,
			transferPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := TransferSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testTransfersUpsert(t *testing.T) {
	t.Parallel()

	if len(transferAllColumns) == len(transferPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := Transfer{}
	if err = randomize.Struct(seed, &o, transferDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Transfer: %s", err)
	}

	count, err := Transfers().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, transferDBTypes, false, transferPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Transfer struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Transfer: %s", err)
	}

	count, err = Transfers().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
