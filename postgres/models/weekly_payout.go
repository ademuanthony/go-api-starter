// Code generated by SQLBoiler 4.7.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// WeeklyPayout is an object representing the database table.
type WeeklyPayout struct {
	ID     string `boil:"id" json:"id" toml:"id" yaml:"id"`
	Date   int64  `boil:"date" json:"date" toml:"date" yaml:"date"`
	Amount int64  `boil:"amount" json:"amount" toml:"amount" yaml:"amount"`

	R *weeklyPayoutR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L weeklyPayoutL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WeeklyPayoutColumns = struct {
	ID     string
	Date   string
	Amount string
}{
	ID:     "id",
	Date:   "date",
	Amount: "amount",
}

var WeeklyPayoutTableColumns = struct {
	ID     string
	Date   string
	Amount string
}{
	ID:     "weekly_payout.id",
	Date:   "weekly_payout.date",
	Amount: "weekly_payout.amount",
}

// Generated where

var WeeklyPayoutWhere = struct {
	ID     whereHelperstring
	Date   whereHelperint64
	Amount whereHelperint64
}{
	ID:     whereHelperstring{field: "\"weekly_payout\".\"id\""},
	Date:   whereHelperint64{field: "\"weekly_payout\".\"date\""},
	Amount: whereHelperint64{field: "\"weekly_payout\".\"amount\""},
}

// WeeklyPayoutRels is where relationship names are stored.
var WeeklyPayoutRels = struct {
}{}

// weeklyPayoutR is where relationships are stored.
type weeklyPayoutR struct {
}

// NewStruct creates a new relationship struct
func (*weeklyPayoutR) NewStruct() *weeklyPayoutR {
	return &weeklyPayoutR{}
}

// weeklyPayoutL is where Load methods for each relationship are stored.
type weeklyPayoutL struct{}

var (
	weeklyPayoutAllColumns            = []string{"id", "date", "amount"}
	weeklyPayoutColumnsWithoutDefault = []string{"id", "date", "amount"}
	weeklyPayoutColumnsWithDefault    = []string{}
	weeklyPayoutPrimaryKeyColumns     = []string{"id"}
)

type (
	// WeeklyPayoutSlice is an alias for a slice of pointers to WeeklyPayout.
	// This should almost always be used instead of []WeeklyPayout.
	WeeklyPayoutSlice []*WeeklyPayout

	weeklyPayoutQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	weeklyPayoutType                 = reflect.TypeOf(&WeeklyPayout{})
	weeklyPayoutMapping              = queries.MakeStructMapping(weeklyPayoutType)
	weeklyPayoutPrimaryKeyMapping, _ = queries.BindMapping(weeklyPayoutType, weeklyPayoutMapping, weeklyPayoutPrimaryKeyColumns)
	weeklyPayoutInsertCacheMut       sync.RWMutex
	weeklyPayoutInsertCache          = make(map[string]insertCache)
	weeklyPayoutUpdateCacheMut       sync.RWMutex
	weeklyPayoutUpdateCache          = make(map[string]updateCache)
	weeklyPayoutUpsertCacheMut       sync.RWMutex
	weeklyPayoutUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single weeklyPayout record from the query.
func (q weeklyPayoutQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WeeklyPayout, error) {
	o := &WeeklyPayout{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for weekly_payout")
	}

	return o, nil
}

// All returns all WeeklyPayout records from the query.
func (q weeklyPayoutQuery) All(ctx context.Context, exec boil.ContextExecutor) (WeeklyPayoutSlice, error) {
	var o []*WeeklyPayout

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to WeeklyPayout slice")
	}

	return o, nil
}

// Count returns the count of all WeeklyPayout records in the query.
func (q weeklyPayoutQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count weekly_payout rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q weeklyPayoutQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if weekly_payout exists")
	}

	return count > 0, nil
}

// WeeklyPayouts retrieves all the records using an executor.
func WeeklyPayouts(mods ...qm.QueryMod) weeklyPayoutQuery {
	mods = append(mods, qm.From("\"weekly_payout\""))
	return weeklyPayoutQuery{NewQuery(mods...)}
}

// FindWeeklyPayout retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWeeklyPayout(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*WeeklyPayout, error) {
	weeklyPayoutObj := &WeeklyPayout{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"weekly_payout\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, weeklyPayoutObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from weekly_payout")
	}

	return weeklyPayoutObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WeeklyPayout) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no weekly_payout provided for insertion")
	}

	var err error

	nzDefaults := queries.NonZeroDefaultSet(weeklyPayoutColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	weeklyPayoutInsertCacheMut.RLock()
	cache, cached := weeklyPayoutInsertCache[key]
	weeklyPayoutInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			weeklyPayoutAllColumns,
			weeklyPayoutColumnsWithDefault,
			weeklyPayoutColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(weeklyPayoutType, weeklyPayoutMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(weeklyPayoutType, weeklyPayoutMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"weekly_payout\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"weekly_payout\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into weekly_payout")
	}

	if !cached {
		weeklyPayoutInsertCacheMut.Lock()
		weeklyPayoutInsertCache[key] = cache
		weeklyPayoutInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the WeeklyPayout.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WeeklyPayout) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	weeklyPayoutUpdateCacheMut.RLock()
	cache, cached := weeklyPayoutUpdateCache[key]
	weeklyPayoutUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			weeklyPayoutAllColumns,
			weeklyPayoutPrimaryKeyColumns,
		)

		if len(wl) == 0 {
			return 0, errors.New("models: unable to update weekly_payout, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"weekly_payout\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, weeklyPayoutPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(weeklyPayoutType, weeklyPayoutMapping, append(wl, weeklyPayoutPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update weekly_payout row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for weekly_payout")
	}

	if !cached {
		weeklyPayoutUpdateCacheMut.Lock()
		weeklyPayoutUpdateCache[key] = cache
		weeklyPayoutUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q weeklyPayoutQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for weekly_payout")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for weekly_payout")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WeeklyPayoutSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), weeklyPayoutPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"weekly_payout\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, weeklyPayoutPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in weeklyPayout slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all weeklyPayout")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WeeklyPayout) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no weekly_payout provided for upsert")
	}

	nzDefaults := queries.NonZeroDefaultSet(weeklyPayoutColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	weeklyPayoutUpsertCacheMut.RLock()
	cache, cached := weeklyPayoutUpsertCache[key]
	weeklyPayoutUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			weeklyPayoutAllColumns,
			weeklyPayoutColumnsWithDefault,
			weeklyPayoutColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			weeklyPayoutAllColumns,
			weeklyPayoutPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert weekly_payout, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(weeklyPayoutPrimaryKeyColumns))
			copy(conflict, weeklyPayoutPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"weekly_payout\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(weeklyPayoutType, weeklyPayoutMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(weeklyPayoutType, weeklyPayoutMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert weekly_payout")
	}

	if !cached {
		weeklyPayoutUpsertCacheMut.Lock()
		weeklyPayoutUpsertCache[key] = cache
		weeklyPayoutUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single WeeklyPayout record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WeeklyPayout) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no WeeklyPayout provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), weeklyPayoutPrimaryKeyMapping)
	sql := "DELETE FROM \"weekly_payout\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from weekly_payout")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for weekly_payout")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q weeklyPayoutQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no weeklyPayoutQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from weekly_payout")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for weekly_payout")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WeeklyPayoutSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), weeklyPayoutPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"weekly_payout\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, weeklyPayoutPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from weeklyPayout slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for weekly_payout")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WeeklyPayout) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWeeklyPayout(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WeeklyPayoutSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WeeklyPayoutSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), weeklyPayoutPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"weekly_payout\".* FROM \"weekly_payout\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, weeklyPayoutPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in WeeklyPayoutSlice")
	}

	*o = slice

	return nil
}

// WeeklyPayoutExists checks if the WeeklyPayout row exists.
func WeeklyPayoutExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"weekly_payout\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if weekly_payout exists")
	}

	return exists, nil
}
